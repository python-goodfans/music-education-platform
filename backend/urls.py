from rest_framework.routers import DefaultRouter\n\n# Import your viewsets here\nfrom .views import UserViewSet, ProfileViewSet, FileViewSet, SubjectViewSet, ArticleViewSet, CompetitionViewSet, ActivityViewSet, ParticipantViewSet\n\nrouter = DefaultRouter()\n\n# Register viewsets with the router\nrouter.register(r'users', UserViewSet)\nrouter.register(r'profiles', ProfileViewSet)\nrouter.register(r'files', FileViewSet)\nrouter.register(r'subjects', SubjectViewSet)\nrouter.register(r'articles', ArticleViewSet)\nrouter.register(r'competitions', CompetitionViewSet)\nrouter.register(r'activities', ActivityViewSet)\nrouter.register(r'participants', ParticipantViewSet)\n\n# The URL patterns can be included in the main urlconf as follows:\n# urlpatterns = router.urls